import { Injectable } from '@angular/core';
import { FirebaseService } from './firebase.service';

@Injectable({
  providedIn: 'root'
})
export class ServizioProvaService {

  // reviews = []
  // titles!:any

  // constructor(private firebase: FirebaseService) { }

  // this.temp = this.firebase.getData(this.firebase.urlGames).subscribe(data => {
  //   this.titles.push(data)
  // })

  // reviews = this.firebase.getReviews(url)
}
